# Dependencies
node_modules/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Runtime / temp
*.tmp
*.temp
*.swp
*.swo

# Python artifacts (if you use python -m http.server etc.)
__pycache__/
*.py[cod]

# Environment files (keep secrets out of git)
.env
.env.*

# OS files
.DS_Store
Thumbs.db
Desktop.ini

# Editors
.vscode/
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/launch.json
!.vscode/tasks.json

# Build outputs (if you add a build step later)
dist/
build/

# Netlify note: The catalogue preview folders are currently committed to git.
# If you want a cleaner repository, you can uncomment the lines below to ignore
# generated preview pages (Netlify will generate them fresh on every deploy).
# However, this means the files won't be available when viewing the repo directly.
#
# To enable this optimization:
# 1. Uncomment the lines below
# 2. Run: git rm -r --cached catalogue/*/
# 3. Run: git add catalogue/_shared/ catalogue/*.js -f
# 4. Commit the changes
#
# catalogue/*/
# !catalogue/_shared/
